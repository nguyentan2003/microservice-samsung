server:
  port: 8003
  servlet:
    context-path: /order

spring:
  application:
    name: order-service
  datasource:
    url: "jdbc:mysql://localhost:3306/topic9_order_service"
    driverClassName: "com.mysql.cj.jdbc.Driver"
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

