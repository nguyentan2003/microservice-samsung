server:
  port: 8006
  servlet:
    context-path: /shipping

spring:
  application:
    name: shipping-service
  datasource:
    url: "jdbc:mysql://localhost:3306/topic9_shipping_service"
    driverClassName: "com.mysql.cj.jdbc.Driver"
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

